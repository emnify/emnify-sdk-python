FROM python:alpine

WORKDIR /sdk

COPY req.txt req.txt

RUN pip install -r req.txt

COPY . .
# docker build . -f Dockerfile.dev -t emnify/python-sdk
# docker run -t -v $(pwd):/docs emnify/python-sdk pytest
CMD echo "Enter Command"
